import numpy as np
from sklearn.mixture import GaussianMixture

X = np.array([[0.1],[0.5],[1.1],[5.0],[6.0],[6.5]])
gmm = GaussianMixture(n_components=2).fit(X)
labels = gmm.predict(X)
print("Cluster assignments:",labels)
print("Means:",gmm.means_.ravel())

